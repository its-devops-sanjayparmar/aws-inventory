# Configuration Guide

The application reads settings from config.yaml and supports environment overrides in future iterations.

## Key settings
- output_dir: directory where reports are written
- regions: default AWS regions to scan
- profile: default AWS profile
- max_workers: concurrency for parallel scanners
- log_level: logging verbosity
